$schema: "https://moonrepo.dev/schemas/project.json"

type: library
language: "go"

project:
  name: "{{ name }}"
  description: "{{ description }}"

workspace:
  inheritedTasks:
    exclude:
      - build

fileGroups:
  sources:
    - "go.mod"
    - "**/*.go"
