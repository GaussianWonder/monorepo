$schema: "https://moonrepo.dev/schemas/project.json"

type: library
platform: node
language: "typescript"

project:
  name: "{{ name }}"
  description: "{{ description }}"

fileGroups:
  sources:
    - "src/**/*"

tasks:
  typecheck:
    outputs:
      - "lib"
